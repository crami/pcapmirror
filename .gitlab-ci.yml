
stages:
  - build
  
variables:
  DEBIAN_FRONTEND: noninteractive

build-bookworm:
  stage: build
  tags:
    - bookworm
  script:
    - tar -czvf ../pcapmirror_0.2.orig.tar.gz --exclude=debian .
    - debuild -us -uc
    - mkdir -p build
    - mv ../pcapmirror*.* build/

  artifacts:
    paths:
      - build/*.deb
      - build/*.dsc
      - build/*.tar.xz
      - build/*.changes
      - build/*.buildinfo
      - build/*.diff.gz
